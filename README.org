#+title: info-nav
#+startup: indent show2levels
#+filetags: :info:emacs:

#+begin_export html
<a href="https://melpa.org/#/info-nav"><img alt="MELPA" src="https://melpa.org/packages/info-nav-badge.svg"/></a>
#+end_export

* About

- This is an alternative to the [[https://www.gnu.org/software/emacs/manual/html_node/info/Create-Info-buffer.html][info-display-manual]] function (which is usually bound to ~C-h R~).
- This also displays an info manual, but it uses two buffers and two windows to do so.
- The left window contains the table of contents.
- The right window contains the contents.
- Actions on the left will drive navigation on the right.

[[./2025-12-18_15-37-05.screenshot.png]]

#+begin_quote
I think this makes info manuals significantly more approachable, especially if one is not familiar with info's keybindings.
#+end_quote

* Installation
** MELPA

[[https://github.com/melpa/melpa/pull/9778][Soon™]]

#+begin_src elisp
(use-package info-nav
  :ensure t
  :bind (("C-h 3" . info-nav)))
#+end_src

** Git

#+begin_src elisp
(use-package info-nav
  :vc (:url "https://codeberg.org/ggxx/info-nav" :rev :newest)
  :bind (("C-h 3" . info-nav)))
#+end_src

*  Usage

** =C-h 3=

The recommended binding is ~C-h 3~.  The reason I chose this was because it reminds me of how ~C-x 3~ splits a window in half.  It's also likely to be free and unbound.

** =M-x info-nav=

If you don't want to bind a key, you can just run it via ~M-x info-nav~.
